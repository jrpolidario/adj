<%= simple_form_for(@card) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :image %>
    <%= image_tag f.object.image.url if f.object.image.present? %>
    <%= f.hidden_field :image_cache %>
    <%= f.input :subimage %>
    <%= image_tag f.object.subimage.url if f.object.subimage.present? %>
    <%= f.hidden_field :subimage_cache %>
    <%= f.input :image_source_url %>
    <%= f.input :score %>
    <%= f.association :category %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
